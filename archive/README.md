# archive/

Material kept with the project for completeness but **not** part of the graded submission.

| Item | What it is | Why archived |
|------|------------|--------------|
| `CS 490 Spring 2026 Syllabus.pdf` | The class syllabus | Reference material — not student work, not a deliverable. |
| `autotraq-dump.sql` (5.5 MB) | Full MySQL database dump | Too large for submission and contains seed data already represented in code/scripts. |
| `Initial DB Script/` | Early SQLAlchemy/Python schema sketches and a sqlite `.db` | Predates the real Prisma backend — kept to show the project's evolution from prototype to production. |
| `visualizations/` | Standalone D3.js visualization stubs (`inventoryBarChart.js`, etc.) | Never wired into the React app (the dashboard uses Recharts). Kept as exploratory work. |

If something here is useful to someone reviewing the project, it's still in the repo — just not in `submission/`.
